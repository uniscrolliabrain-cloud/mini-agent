# Copia a frontend/.env (esta ignorado por git).
# URL de la API. En desarrollo puedes dejarlo vacio: vite hace proxy de /api -> http://localhost:8000
VITE_API_URL=
# Debe coincidir con ADMIN_API_KEY del backend.
VITE_API_KEY=
